#!/bin/sh
#
# Прогон локальных наборов данных модуля.
#
# Прежняя версия публиковала кадры v2 в waf.data.* сама и проверяла край
# nginx-1 риги разработчика. С keeper (docs/keeper.md) в основной subject
# набора не пишет никто, кроме keeper, а риги на стенде нет: провод v3
# проверяется сквозняком через keeper и управляемые края --
# tests/keeper/keeper.sh (запись событием с ответом, дельта на всех краях,
# waf_local_check, снятие, истечение, новая эпоха, сверка хешей). Разбор и
# сборка наборов на C проверяются unit-конфигами: node nginx/tests/unit/run.mjs.
#
#     sh tests/dataset/dataset.sh

exec sh "$(dirname -- "$0")/../keeper/keeper.sh" "$@"
