# Калитка «только код доступа»: политика при источнике code.

mode: enforce

source: code

gate:
  redirect_methods: [GET, HEAD]
  redirect_status: 303
  deny_response: auth_required
  html_only: true
