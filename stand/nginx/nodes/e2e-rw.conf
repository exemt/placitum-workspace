# Одноразовая нода e2e модификатора ответов (tests/rewrite): конфиг лежит
# файлом (deploy/nginx/nginx.conf), поэтому шину задаёт нода -- как у снесённых
# nginx-1..3. Управляемые edge-01..03 к этой ноде отношения не имеют.
waf_node_id e2e-rw;
waf_bus nats://nats:4222 payload_max=16m;
