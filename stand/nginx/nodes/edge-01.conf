# Нода 1 -- управляемая контроллером. Здесь только идентичность: шину печатает
# поколение (waf_http.bus пространства), и второй waf_bus из этого файла делал
# бы `nginx -t` ошибкой "directive is duplicate".
waf_node_id edge-01;
