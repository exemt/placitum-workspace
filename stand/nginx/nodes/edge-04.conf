# Нода 4 -- управляемая контроллером (edge-managed). Здесь остаётся только
# идентичность: шину печатает поколение (waf_http.bus пространства), и второй
# waf_bus из этого файла делал бы `nginx -t` ошибкой "directive is duplicate".
#
# На nginx-1..3 наоборот: там конфиг лежит файлом и шину задаёт нода.
waf_node_id edge-04;
